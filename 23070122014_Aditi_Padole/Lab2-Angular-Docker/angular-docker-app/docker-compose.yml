version: '3.8'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports: ["4200:4200"]
    volumes: ["./:/app", "/app/node_modules"]
  prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports: ["8081:80"]
version: "3.9"

services:
  angular-app:
    build: .
    container_name: angular-dev

    ports:
      - "4200:4200"

    volumes:
      - .:/app
      - /app/node_modules

    command: npm start -- --host 0.0.0.0
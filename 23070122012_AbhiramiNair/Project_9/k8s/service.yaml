apiVersion: v1
kind: Service
metadata:
  name: apache2-service
  namespace: apache-demo
spec:
  selector:
    app: apache2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort

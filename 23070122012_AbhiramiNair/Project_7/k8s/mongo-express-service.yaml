apiVersion: v1
kind: Service
metadata:
  name: mongo-express-service
  namespace: mongo-demo
spec:
  selector:
    app: mongo-express
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: NodePort

services:
  react-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules

  react-prod:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "8081:80"

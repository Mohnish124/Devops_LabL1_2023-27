apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
stringData:
  # stringData keeps this lab manifest readable. Kubernetes stores the values as Secret data.
  mongodb-root-username: admin
  mongodb-root-password: change-me-in-production

# Screenshot Checklist - Assignment TW1.1: Git Workflow (Minimal 3-Screenshot Set)

This document lists the **3 minimal execution screenshots** required for **Assignment TW1.1**.

---

## Required Execution Proof Screenshots

| Screenshot ID | Filename | Content / What it Contains | Why it is Sufficient Evidence |
| :--- | :--- | :--- | :--- |
| `TW1.1_SS_01` | `TW1.1_01_git_init_branch_push.png` | Terminal output showing `git init`, initial commit, `git checkout -b feature/user-auth`, feature commit, and `git push origin feature/user-auth`. | Proves repo setup, initial commit, branching, and feature branch push. |
| `TW1.1_SS_02` | `TW1.1_02_merge_conflict_resolution.png` | Terminal output showing `git merge feature/user-auth` conflict alert, manual resolution commit, and `git push origin main`. | Proves conflict simulation, manual resolution, and merge commit. |
| `TW1.1_SS_03` | `TW1.1_03_git_log_graph.png` | Terminal output of `git log --graph --oneline --all` displaying commit history tree. | Proves commit topology, branching history, and merge resolution lineage. |

# Screenshot Checklist - Assignment TW1.1: Git Workflow

This document lists the verified execution screenshots for **Assignment TW1.1: Git Workflow**.

---

## Verified Execution Proof Screenshots

| Status | Screenshot ID | Filename | Content / What it Contains | Why it is Sufficient Evidence |
| :---: | :--- | :--- | :--- | :--- |
| ✅ | `TW1.1_SS_01` | `TW1.1_01_git_init_branch_push.png` | Terminal output showing `git init`, initial commit, `git checkout -b feature/user-auth`, feature module commit, and `git push -u origin feature/user-auth`. | Proves Task 1.1 repo setup & initial commit AND Task 1.2 feature branching and remote push. |
| ✅ | `TW1.1_SS_02` | `TW1.1_02_merge_conflict_resolution.png` | Terminal output showing `git merge feature/user-auth` resulting in merge conflict, code editor showing conflict resolution, manual merge commit, and `git push origin main`. | Proves Task 1.3 parallel branch merge conflict simulation, manual resolution, and merge push. |
| ✅ | `TW1.1_SS_03` | `TW1.1_03_git_log_graph.png` | Terminal output of `git log --graph --oneline --all` displaying commit history tree (`d8f6c3a` merge resolution, `a91b3ef` feature commit, `4b2c7de` initial commit). | Proves commit topology, branching history, and merge resolution lineage. |

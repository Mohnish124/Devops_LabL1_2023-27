FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 4200
# Run the Angular development server and expose it to the host
CMD ["npx", "ng", "serve", "--host", "0.0.0.0"]